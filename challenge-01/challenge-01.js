const myvar = 10;
const soma = 15 + 8;
const souninja = true;
const comida = ['arroz', 'feijão' , 'ovo']

soma += 1;
soma *= 3;

comida[1]

soma === myvar ? true : false;

myvar <= soma ? true : false;

function divisao(num1, num2){
   return num1/num2;
}


divisao(10, 2);
